# 👥 دليل الحسابات وصلاحيات المستخدمين المعتمدة (User Accounts & Granular RBAC)
**نظام إدارة الملاك والموقف اليومي — شعبة تبريد المركز ومحطة عزل نهر بن عمر — شركة نفط البصرة**  
*الإصدار المعتمد: v8.5 Enterprise Cloud Master Suite 💎 | تاريخ التوثيق: سبتمبر 2026*

---

## 📑 فهرس الدليل
1. [الحسابات](#1-الحسابات)
2. [مصفوفة الصلاحيات](#2-مصفوفة-الصلاحيات)
3. [الأدوار الأربعة](#3-الأدوار-الأربعة)
4. [الأمان](#4-الأمان)
5. [قفل التحرير بين الأقران](#5-قفل-التحرير-بين-الأقران)
6. [دليل المدير](#6-دليل-المدير)

---

## 1. الحسابات

الدخول إلى النظام حساب Firebase موثّق ببريد إلكتروني وكلمة مرور، لا رمز مرور مشترك ولا حساب افتراضي. الحساب **يُنشأ** من لوحة Firebase Console، ثم **يُربط** داخل النظام بلصق معرّف المستخدم (`UID`) الخاص به في نافذة إدارة الحسابات، حيث يُحدَّد الدور والصلاحيات وأولوية التحرير.

| اسم المستخدم | البريد | الدور | أولوية التحرير | UID |
| :--- | :--- | :---: | :---: | :--- |
| أسامة خليل | usama.kh1975@gmail.com | `admin` (مدير النظام 👑) | — | يُنسخ من Firebase |
| قاسم صبري | qasim@hr-cooling.local | `operator` (مُدخل بيانات ✍️) | 1 | يُنسخ من Firebase |
| حيدر سعد | haidar@hr-cooling.local | `operator` (مُدخل بيانات ✍️) | 2 | يُنسخ من Firebase |

> [!NOTE]
> لا يوجد تسجيل ذاتي (Self-Signup) في هذا النظام بتاتاً. كل حساب جديد ينشئه مدير النظام يدوياً من لوحة Firebase، ثم يربطه أحد المديرين داخل النظام. أولوية التحرير رقم صحيح (الأصغر أعلى أولوية) ويُستخدم فقط بين حسابات تتشارك نفس الصلاحية — انظر القسم 5.

---

## 2. مصفوفة الصلاحيات

| الإجراء | `admin` | `manager` | `operator` | `viewer` |
| :--- | :---: | :---: | :---: | :---: |
| الاطلاع والبحث والطباعة والتصدير | ✅ | ✅ | ✅ | ✅ |
| تعديل الموقف اليومي ومطابقة الوجبات | ✅ | ✅ | 🔒 بالصلاحية | ❌ |
| تعديل الملاك والسلامة والتقييم | ✅ | ✅ | 🔒 بالصلاحية | ❌ |
| حذف موظف من الملاك | ✅ | ✅ مع تدقيق | ❌ | ❌ |
| فك قفل الجلسات 🔓 | ✅ | ✅ | ❌ | ❌ |
| الاطلاع على الأرشيف وتنزيل لقطة | ✅ | ✅ | ❌ | ❌ |
| استعادة لقطة كقاعدة حية | ✅ | ❌ | ❌ | ❌ |
| حذف قاعدة البيانات الشامل | ✅ | 📨 طلب موافقة | 📨 طلب موافقة | ❌ |
| إدارة الحسابات والأدوار | ✅ | ❌ | ❌ | ❌ |
| أدوات المطوّر F12 | ✅ | ❌ | ❌ | ❌ |

مسار «طلب الموافقة» قائم في النظام أصلاً: غير المدير يقدّم طلباً بسبب مكتوب، فيظهر للمدير شريط موافقة أو رفض.

---

## 3. الأدوار الأربعة

| المفتاح | المسمّى | ما يقابله مؤسسياً | جوهر الدور |
| :--- | :--- | :--- | :--- |
| `admin` | مدير النظام 👑 | System / Security Administrator | الهوية والتعافي والتدمير، إضافةً لكل صلاحيات الإداري |
| `manager` | إداري 🛡️ | HR Functional Administrator | سلطة كاملة على البيانات، بلا هوية ولا تعافٍ ولا F12 |
| `operator` | مُدخل بيانات ✍️ | Data Entry Clerk | تبويبات محدّدة يمنحها له المدير |
| `viewer` | مستعرض 👁️ | Read-only / Auditor | اطلاع وبحث وطباعة وتصدير فقط |

**تغيير تسمية:** `operator` كان يُعرض باسم «إداري مُدخل»، ويصبح «مُدخل بيانات» لإزالة اللبس مع الدور الجديد. المفتاح `operator` لا يتغيّر، فلا تتأثر الحسابات القائمة.

---

## 4. الأمان

* **تسجيل الدخول:** Firebase Authentication ببريد إلكتروني وكلمة مرور. لا يوجد أي رمز مرور مشترك أو رمز ماستر في النظام على الإطلاق.
* **الأدوار من الخادم:** دور كل حساب وصلاحياته لا يُخزَّنان في المتصفح، بل في عقدة `roles/{uid}` على خادم Firebase، وتُقرأ عند كل تسجيل دخول. لا يستطيع أي حساب تعديل دوره أو صلاحياته من المتصفح، حتى لو عدّل الكود محلياً.
* **سجل التدقيق (Audit Log):** كل عملية حساسة (حفظ، حذف، فك قفل، حذف قاعدة) تُسجَّل في `audit_log` بشكل **إضافة فقط**. لا يستطيع أي حساب — بما فيه حساب المدير — تعديل قيد موجود أو حذفه، لا من الواجهة ولا حتى بالوصول المباشر إلى قاعدة البيانات، لأن قواعد Firebase نفسها ترفض ذلك.
* **قفل الجلسة الفردية:** لا يمكن فتح نفس الحساب من جهازين في آن واحد. يرسل المتصفح نبضاً حياً (`Heartbeat`) كل 5 ثوانٍ إلى `active_sessions/{uid}`، وإذا انقطع النبض (خروج أو إغلاق متصفح) يتحرر الحساب تلقائياً خلال 15 ثانية. مدير النظام والإداري يملكان أيضاً زر فك قفل فوري لأي جلسة معلّقة.

---

## 5. قفل التحرير بين الأقران

قاسم وحيدر يحملان اليوم نفس الدور ونفس الصلاحية (`dailyReport`). إن عملا على نفس اليوم معاً، ضاع عمل أحدهما بلا أثر. التنبيه وحده لا يكفي: من يراه قد يتجاهله.

**الآلية المعتمدة** — قفل سجل على غرار ما تفعله الأنظمة المؤسسية:

1. يحمل كل حساب حقلاً رقمياً `editPriority` (الأصغر أعلى أولوية): قاسم = 1، حيدر = 2. يُخزَّن في `roles/{uid}` ويُعدَّل من نافذة إدارة الحسابات — فلا يحتاج تغيير الترتيب أي تعديل برمجي، وهو ما يناسب وضعاً وصفه المستخدم بأنه مؤقت.
2. تُبَث `permissions` و `editPriority` ضمن سجل الجلسة في `active_sessions/{uid}`، فيرى كل جهاز أقرانه المتصلين.
3. لكل قسم (`dailyReport` وغيره): إن وُجدت جلسة حيّة (نبضها خلال 15 ثانية) لقرين يملك نفس القسم و `editPriority` أصغر، يهبط هذا المستخدم إلى **الاطلاع فقط في ذلك القسم وحده**، مع شريط يوضّح السبب: «قاسم صبري يعدّل الموقف اليومي الآن — أنت في وضع الاطلاع».
4. حين تنقضي جلسة صاحب الأولوية (خروج أو إغلاق متصفح)، ترتفع القيود تلقائياً خلال 15 ثانية دون أي تدخل.

**استثناء المشرفين:** لا يشارك `admin` ولا `manager` في هذه المزاحمة — لا يُخفَّضان ولا يُخفِّضان غيرهما. السبب عملي: مدير يترك تبويبه مفتوحاً طوال اليوم كان سيقفل المُدخلين عن العمل بلا مبرر. يبقى احتمال تعارض بين مشرف ومُدخل، ويلتقطه فحص تحديث السحابة الأخير كشبكة أمان.

**نطاق القفل قسم واحد لا النظام كله:** من يفقد التعديل في «الموقف اليومي» يبقى قادراً على العمل في أي قسم آخر يملكه، وعلى الاطلاع والطباعة والتصدير في كل شيء.

---

## 6. دليل المدير

للوصول إلى إدارة الحسابات:
1. الضغط على زر **`👥 إدارة الحسابات والمستخدمين`** في شريط الترويسة العلوي، أو من بطاقة **"الإجراءات السريعة"** في لوحة التحكم. النافذة متاحة لمدير النظام فقط.
2. **إنشاء حساب جديد:**
   * يُنشئ مدير النظام الحساب يدوياً من لوحة Firebase Console (بريد إلكتروني وكلمة مرور)، إذ لا يوجد تسجيل ذاتي داخل النظام.
   * يُنسخ معرّف المستخدم (`UID`) للحساب الجديد من لوحة Firebase.
3. **ربط الحساب داخل النظام:**
   * لصق الـ `UID` في نافذة إدارة الحسابات وكتابة اسم المستخدم.
   * تحديد الدور (`مدير النظام 👑` أو `إداري 🛡️` أو `مُدخل بيانات ✍️` أو `مستعرض 👁️`).
   * لدور `مُدخل بيانات`: تفعيل الصلاحيات المخصصة للتبويبات (الموقف اليومي، الملاك، السلامة، التقييم) وتعيين أولوية التحرير عند تشارك الصلاحية مع حساب آخر.
   * الضغط على **"💾 حفظ وتثبيت المستخدم"**.

> [!IMPORTANT]
> لا يمكن حذف آخر حساب مدير نظام نشط، ولا تعطيله، ولا تنزيل رتبته إلى دور آخر. فقدان آخر حساب مدير يُقفل إدارة الحسابات نهائياً بلا أي مخرج داخل النظام، والحل الوحيد عندها تدخل يدوي من لوحة Firebase.

---

**✍️ إعداد وتوثيق: المهندس أسامة خليل هاشم الزويني**  
*نظام إدارة الملاك والموقف اليومي المتكامل — شركة نفط البصرة*
