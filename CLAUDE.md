# نظام إدارة الملاك — تعليمات المشروع

نظام إدارة ملاك (شؤون موظفين) لشعبة تبريد المركز ومحطة عزل نهر بن عمر التابعة لشركة نفط البصرة. تطبيق React بملف واحد (`index.html`، حالياً حوالي 900KB)، بلا خطوة بناء (build step) — Babel يعمل داخل المتصفح مباشرة عبر `<script type="text/babel">`. لا تضف حزم npm أو أدوات بناء.

## بداية كل جلسة — راجع الذاكرة الدائمة أولاً

قبل أول رد أو حل مقترح في أي محادثة (خصوصاً محادثة جديدة تبدأ بعد `/compact`): راجع فعلياً `MEMORY.md` في مجلد الذاكرة الدائمة الخاص بالمستخدم — فهرس قواعد وتفضيلات متراكمة من جلسات سابقة (مثل تسمية الجلسة الجديدة باسم يلائم المهمة القادمة فور معرفة محتواها، والإبلاغ الدوري بتقدّم الاختبارات أثناء تشغيلها، وغيرها). لا تكتفِ بافتراض أنه "مُحمَّل تلقائياً في السياق" — التحميل التلقائي لا يعني التطبيق: حدث فعلاً أن نُسيت قاعدة منه (تسمية الجلسة بعد تلخيص محادثة سابقة) رغم توفّرها في السياق. اعتبر مراجعته خطوة واعية في بداية كل جلسة، لا افتراضاً سلبياً.

## الملفات الثلاثة الحية

- **`index.html`** — النسخة الأساسية/المصدر. كل تعديل منطقي يبدأ هنا.
- **`نظام_ادارة_الملاك_v9.5_cloud.html`** — يجب أن تبقى **مطابقة تماماً بايت ببايت** لـ `index.html` دائماً. بعد أي تعديل: `cp index.html "نظام_ادارة_الملاك_v9.5_cloud.html"`.
- **`نظام_ادارة_الملاك_v9.5_offline.html`** — نسخة مستقلة تماماً بتسجيل دخول محلي برمز (PIN)، **صفر إشارات** إلى Firebase (`firebaseio`, `identitytoolkit`, `AIza`). **لا تُنسخ من `index.html` أبداً** — أي تعديل منطقي/واجهة غير مرتبط بالسحابة يُنقَل (يُرحَّل) يدوياً بتعديل مطابق، مع الحفاظ على استقلاليتها. تحقّق: `grep -c "firebaseio\|identitytoolkit\|AIza" "نظام_ادارة_الملاك_v9.5_offline.html"` يجب أن يكون **0**.

## الاختبارات الآلية — أُوقفت بطلب صريح من المستخدم (2026-09-16)

**لا تُشغِّل أي اختبار آلي (لا البوابتين أدناه، ولا `scratch/run_suite.sh`، ولا اختبارات Playwright مخصَّصة جديدة) كخطوة تلقائية افتراضية بعد أي تعديل.** الاعتماد الآن على مراجعة المساعدين الخارجيين وحدها (القسم التالي) قبل اعتبار أي تعديل جاهزاً للالتزام. السبب: خلال تنفيذ مرحلة عبر منهجية TDD-لكل-مهمة (اختبار Playwright مخصَّص + وكيل منفِّذ + وكيل مراجع لكل مهمة، مهما صغرت)، استغرقت مرحلة واحدة يومين كاملين بدل جلسة واحدة كانت تكفي لمراحل مماثلة سابقاً — قال المستخدم: «كثرة الاختبارات قد اخرت انجاز المهام... اوقف كل الاختبارات لا داعي لها اعرضها على المساعدين الخرجيين وهذا يكفي»، وأكّد صراحةً حين سُئل أن هذا يشمل حتى البوابتين الفوريتين أدناه رغم أنهما ثوانٍ معدودة.

**هذا لا يلغي المعرفة التقنية وراء البوابتين — تبقى صالحة لو احتاج المستخدم تشغيلها يدوياً مستقبلاً:**

```bash
node scratch/check_babel_block.js <file>   # بناء الجملة (syntax) فقط
node scratch/smoke_render.js <file>        # يُنفّذ المكوّن فعلياً — يكشف أخطاء التنفيذ (TDZ، متغيرات غير معرَّفة)
```

بوابة بناء الجملة وحدها **لا تكفي** لو شُغِّلت — عطل تعريف متغيّر متأخر (TDZ) تسبّب سابقاً بشاشة بيضاء كاملة للتطبيق ونجا من عشر مراجعات كود لأن أحداً لم يُشغّل الكود فعلياً. لكن تشغيلهما لم يعد خطوة افتراضية — فقط عند طلب صريح من المستخدم.

**تحذير ترتيب التصريح (Declaration order):** القيمة الأولية لـ `useState`/`useMemo` ومصفوفة الاعتماديات تُنفَّذ أثناء الرسم، فأي شيء تقرؤه يجب أن يكون معرَّفاً قبله. الدوال التي تُستدعى لاحقاً فقط (داخل معالِج حدث مثلاً) يمكن أن تُعرَّف بعده، بفضل الإغلاق (closure) — لكن لا تعتمد على هذا عند القيم الأولية.

## البوابة الثالثة: استدعاء المساعدين باستقلالية (بلا انتظار إذن مسبق)

كل تعديل جوهري (ميزة، أو إصلاح عطل، أو أي تغيير في منطق قائم) يحتاج مراجعة **Codex** قبل اعتباره جاهزاً للالتزام أو الرفع. طبّق سياسة **EngineeringAIHub** لتصنيف المهمة، والتوزيع الثابت للأدوار: **Codex** يراجع الكود، **Kimi** يهاجم التصميم بسيناريوهات الاستخدام الحقيقي، **Gemini** للبحث وقراءة الملفات الكبيرة، **Antigravity** (خادم MCP مستقل، ليس داخل EngineeringAIHub) للمعمارية وتصميم الواجهات. اختيار المزوّد والتوقيت وصياغة ما يُرسَل قرارك أنت وحدك — لا تسأل المستخدم "هل أستدعي Codex؟" ولا تنتظر موافقته في رسالة لاحقة قبل الاستدعاء؛ هذا القيد أُلغي بطلب صريح من المستخدم في 2026-09-12 («اطلق لك العنان في اختيار المساعدين... اعتمد على ذكائك»)، بعد أن رأى فائدة إشراكهم الفعلية في هذا المشروع.

**ما يبقى قائماً رغم هذا التفويض:**
- **بلا بيانات حقيقية:** لا يصل إلى أي مزوّد خارجي اسم موظف حقيقي أو رقم وظيفي حقيقي أو أي بيانات من نسخة احتياطية فعلية — كود، ووصف تصميم، وبيانات مصطنعة فقط دائماً.
- **محاولة واحدة لكل استدعاء:** بلا إعادة محاولة تلقائية، وبلا تحويل إلى مزوّد بديل عند فشل أو تعذّر، وبلا طلب رأي ثانٍ من مزوّد آخر لنفس المهمة — قرار كهذا يحتاج تقييماً جديداً منك في حينه لا تكراراً آلياً.
- **الشفافية بعد لا قبل:** أبلغ المستخدم بما استدعيتَه ولماذا ونتيجته — في تقريرك، لا كطلب إذن مسبق. تعذّر الوصول إلى مزوّد (انقطاع خادم أو مهلة) يُقال صراحة، ولا يُخفى ولا يُعاد بصمت.
- **تنبيه معروف، مقبول عمداً (2026-09-16):** أعطال هذا المشروع الحقيقية سابقاً كانت افتراضات غير مُختبَرة لا أخطاء كتابة — عمود أسماء فارغ بصمت لأن الحقل `item.employee.name` لا `item.name`، وشاشة بيضاء من ترتيب تصريح؛ مراجعة تقرأ قطعة كود لا تكشف هذا النوع بالضرورة، تشغيل الشيء فعلاً بـPlaywright كان يكشفه. المستخدم اطّلع على هذا الخطر تحديداً (البوابتان أعلاه ذُكرتا صراحة) وقرر مع ذلك إيقاف كل تشغيل آلي، مفضِّلاً السرعة والاعتماد الكامل على المراجعة الخارجية. لا تُعِد فرض التشغيل الآلي بحجة هذا الخطر — إن ظهر عطل من هذه الفئة يُصحَّح عند اكتشافه، لا باستباقه بإلزام اختبار لم يعد مطلوباً.

## بعد كل تعديل

```bash
python publish_desktop_copies.py
```

ينسخ `index.html` و`النسخة الأوفلاين` (بعد التحقق من التطابق والعزل) إلى سطح المكتب — وهو ما يختبر عليه المستخدم فعلياً. شغّله **تلقائياً بعد كل تعديل**، لا فقط عند طلب صريح أو ظهور خطأ نسخة قديمة.

## Git — الالتزام باستقلالية، والرفع وحده يبقى بموافقة صريحة

في 2026-09-13 ألغى المستخدم قيد الموافقة الصريحة على كل الجوانب الإجرائية («انا اعطيتك تفويض كامل وأي شرط يتطلب الموافقة اعتبره لاغياً»)، ثم صحّح فوراً في الرسالة التالية: «التفويض يشمل كل النواحي الاجرائية الا الرفع». فالحاصل: **الالتزام (`git commit`) صار بحكمي بلا انتظار إذن، والرفع (`git push`) وحده استثناء يبقى بموافقة صريحة في المحادثة الحالية، كما كان دائماً.**

- **لا اختبارات آلية إلزامية قبل الالتزام (غُيِّر 2026-09-16، انظر القسم أعلاه).** الجاهزية للالتزام تأتي من مراجعة المساعد الخارجي المناسب (البوابة الثالثة أدناه)، لا من تشغيل اختبارات.
- التزامات صغيرة متكرّرة أفضل من التزام ضخم واحد يجمع تغييرات غير مترابطة.
- بعد كل التزام: أبلغ المستخدم بما التُزم (الرسالة ورقم الالتزام) في تقريرك.
- **لا تُشغِّل `git push` أبداً بلا طلب صريح من المستخدم في نفس المحادثة.** فرع `main` يُنشَر مباشرة عبر GitHub Pages ويستخدمه موظفو الشعبة فعلياً — الرفع نشر إنتاجي (deployment) حقيقي، لا حفظ نسخة احتياطية.
- **`scratch/run_suite.sh` لم يعد بوابة إلزامية قبل الرفع (غُيِّر 2026-09-16، انظر قسم "الاختبارات الآلية" أعلاه).** الجاهزية للرفع تأتي من مراجعة المساعدين الخارجيين، لا من تشغيل المجموعة.
- **راجع دائماً `git log` وذاكرة الجلسات قبل افتراض جاهزية أي شيء للنشر** — لمعرفة ما إذا اختُبر شيء سابق في متصفح حقيقي فعلاً بعد آخر رفع.
- **لا سطر "Co-Authored-By" في أي التزام جديد (قرار 2026-09-17).** كان يُضاف افتراضياً (تعليمات خارج هذا المشروع)، لكن المستخدم رأى أن لا فائدة عملية منه في نظام داخلي صغير كهذا («ما الفائدة من ذكر Sonnet او Haiku... العمل صار ملئ بالصعوبات»)، وأن محاولة تصحيح 3 التزامات قديمة تحمل اسم نموذج خاطئ (Haiku بدل Sonnet) كادت تتحوّل لإعادة كتابة تاريخ Git المنشور (force-push على `main` الحيّ) لفائدة توثيقية شبه معدومة. **القرار: لا تُلمَس الالتزامات القديمة إطلاقاً (صفر مخاطرة)، وتتوقف إضافة السطر من الآن فصاعداً فقط.**

## اصطلاحات البيانات والتاريخ

- التواريخ سلاسل نصية بصيغة `YYYY-MM-DD`، تُقارَن معجمياً (`<=`/`>=`) — هذا صالح لهذه الصيغة تحديداً ومتّبع في كل الملف.
- عطلة نهاية الأسبوع: الجمعة والسبت (`getDay() === 5 || getDay() === 6`)، بالإضافة إلى `officialHolidays` (مصفوفة تواريخ محدَّدة يدوياً).
- **استخدم `localDateStr()` دائماً، لا `new Date().toISOString()`** — بغداد بتوقيت UTC+3 بلا توقيت صيفي، و`toISOString()` يُعيد تاريخ UTC الذي يكون "أمس" محلياً بين الساعة 00:00–03:00. هذا الفارق سبّب عطلاً حقيقياً في هذا المشروع.
- أنواع الفترات المؤرَّخة الثمانية (`PERIOD_TYPES`) ثابتة تماماً: `إجازة اعتيادية`، `في دورة`، `إيفاد`، `إجازة بدون راتب`، `إجازة طويلة`، `إجازة أمومة`، `غياب`، `سحب يد`. لا تُضِف نوعاً تاسعاً ولا تُغيِّر التسمية إلا بطلب صريح من المستخدم (كما أُضيف `سحب يد` في 2026-09-14). `غياب` و`سحب يد` وحدهما مفتوحا النهاية (`OPEN_ENDED_PERIOD_TYPES`)، و`سحب يد` يُعامَل مطابقةً تامة للغياب في الإحصائيات وقوائم الماء — أي موضع جديد يعالج `غياب` يجب أن يعالج `سحب يد` معه.
- `emp.status` (النص القديم قبل الفترات المؤرَّخة) لا يزال يُقرأ من عشرات المواضع — لا تحذفه أبداً؛ سجلات الموظفين القديمة بلا `statusPeriods` تعتمد عليه عبر آلية رجوع (fallback) في `getActivePeriod`.

## الوضع الليلي (Dark Mode)

مبني كطبقة CSS مستقلة تماماً (`<style>` بعد `<head>`، ~180 سطراً): تُعيد تعريف كل صنف Tailwind **مُستخدَم فعلياً حالياً** تحت `html[data-theme="dark"] .classname { ... !important }`، مُغلَّفة بـ`@media screen` (الطباعة تبقى بيضاء دائماً — مُتحقَّق منه فعلياً بمحاكاة وسيط الطباعة، لا افتراضاً). المفتاح العام `window.__toggleHrTheme()`.

**القيد الحقيقي الوحيد**: هذه قائمة تعداد يدوي، لا قاعدة عامة. **أي صنف لوني جديد (خلفية/نص/حدّ، بأي درجة أو نسبة شفافية) يُستخدَم في JSX ولم يكن موجوداً وقت كتابة هذه القائمة سيظهر بلونه النهاري الفاتح فوق خلفية داكنة — بصمت، بلا أي خطأ في وحدة التحكم.** عند إضافة تصميم أو مكوّن جديد بألوان لم تُستخدَم من قبل في المشروع، تحقّق يدوياً من ظهوره في الوضع الليلي، أو أضِف صنفه إلى قائمة الإعادة تعريف في `index.html` (ابحث عن "الوضع الليلي (Dark Mode)").

**بديل أمتن مُتاح لو استحق التحوّل مستقبلاً**: Tailwind Play CDN (`cdn.tailwindcss.com` المُستخدَم هنا) يدعم فعلياً نمط `dark:` القياسي عبر `tailwind.config = { darkMode: ['selector', '[data-theme="dark"]'] }` — **يجب أن يأتي هذا السطر بعد وسم سكربت Tailwind لا قبله** (مُتحقَّق منه عملياً). هذا يجعل الصنف الجديد يحمل حالته الليلية معه في المكان نفسه (`className="bg-gray-50 dark:bg-slate-800"`) بدل قائمة مركزية منفصلة قد تُنسى — تحوّل معماري، لا إصلاحاً عاجلاً.

## التوثيق المرجعي في المشروع

- `نظام_ادارة_الملاك_الدليل_الشامل_للقواعد.md` — الدليل الشامل لقواعد النظام (يُحدَّث مع كل ميزة جوهرية).
- `نظام_ادارة_الملاك_دليل_الصلاحيات_وحسابات_المستخدمين.md` — دليل الصلاحيات والحسابات.
- `docs/superpowers/specs/` و`docs/superpowers/plans/` — مواصفات وخطط الميزات الكبرى الموثَّقة عبر عملية العصف الذهني/التخطيط.
- `archive/` — سكربتات ترقية قديمة من إصدارات سابقة (v66–v70) وبنية خادم مهجورة، محفوظة للرجوع التاريخي فقط، لا تُستخدَم.

## اللغة

راسِل المستخدم بالعربية في كل الردود المحادثية، لا الإنجليزية — واجهة النظام وتوثيقه عربيان بالكامل.
